fastapi==0.115.6
uvicorn==0.34.0
requests==2.34.2
python-dotenv==1.2.2
python-multipart==0.0.32
aiofiles==23.2.1
trimesh==4.12.2
numpy==2.5.0
networkx==3.6.1
anthropic>=0.120.0

supabase>=2.7.0
websockets>=13

# Graph memory. Optional at runtime — main.py imports it lazily and degrades to
# "no graph memory" when it is absent, so the kiosk still boots without it.
FalkorDB>=1.6.2

# Live web research on a memory miss. Optional — lazily imported, and the
# Engineer pipeline sizes parts from the model's own knowledge without it.
linkup-sdk>=0.19.0

# Pipeline telemetry over Apache Iggy. Optional — imported lazily inside the
# background worker, so a missing package logs a warning and nothing else.
laser-sdk>=0.0.1

# RocketRide pipeline client, for the parallel design-brief path behind
# ROCKETRIDE_ENABLED. Optional — imported lazily, and unreferenced when the
# flag is off.
rocketride>=1.3.0
